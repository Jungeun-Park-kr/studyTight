function uploadFile(){
    var new_file=document.getElementById('input_file'); 
    var new_div=document.createElement('div');
    new_div.setAttribute('class','new_postit_file');
   // new_div.setAttribute('new'); 
    //파일 이름이 같으면 그 파일을 보여주기. 
}